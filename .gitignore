node_modules/
*.tsbuildinfo
.env
.env.*
# dist/ is built fresh in CI (prepublishOnly) and shipped in the npm tarball —
# no longer committed now that consumers install from npm, not the git repo.
dist/
